import { TabsTitleDirective } from './tabs-title.directive';

describe('TabsTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new TabsTitleDirective();
    expect(directive).toBeTruthy();
  });
});
